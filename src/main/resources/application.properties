spring.application.name=gerenciador-tarefas

# Configuração da Conexão com o Banco de Dados (DataSource)
spring.datasource.url=jdbc:postgresql://localhost:5432/gerenciador
spring.datasource.username=postgres
spring.datasource.password=pgk88433

# Configuração do Hibernate (a implementação do JPA)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect